# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 候选人：在求职准备、技能训练或面试复盘场景中，进行文字、语音或视频模拟面试，查看 AI 反馈，并管理简历、岗位申请、学习资料和练习记录。
- 企业管理员：管理企业岗位、候选人申请、线下面试安排和招聘流程，查看可解释的岗位匹配结果。
- 平台管理员：运营题库、面试场次、候选人、AI Provider、提示词、学习资料、工单、通知和审计信息。

## Product Purpose

AInterview 是多模态智能模拟面试与招聘训练平台。它将候选人练习、AI 追问与评分、报告复盘、算法训练、简历与岗位申请，以及企业和平台运营流程集中在同一套产品中。现有产品成功标准是：候选人能够可靠完成练习并获得可执行的改进依据，企业和平台管理员能够在权限边界内高效管理招聘与训练内容。

## Positioning

暂定定位（根据仓库现有功能归纳，待用户确认）：以多模态模拟面试为核心，连接 AI 评估复盘、算法练习和招聘投递的训练到求职闭环。需要用户后续确认对外应强调“训练闭环”、AI 评估机制，还是其他差异化优势。

## Operating Context

- 候选人通过浏览器登录后进入工作台，继续或创建模拟面试，按题作答并接收追问，完成后查看报告、能力趋势和面试心得。
- 候选人还可进行算法练习、查看授权学习资料、维护私有简历、浏览公开岗位并提交申请，接收站内通知和处理反馈工单。
- 企业管理员在独立的企业工作台中维护岗位、筛选申请、查看候选人详情、推进申请状态并安排线下面试；企业数据按当前登录用户的 `companyId` 隔离。
- 平台管理员通过管理工作台维护题库、面试、候选人、提示词、AI Provider、学习资料、工单、通知和审计日志。
- 当前有效前端为 `frontend-react` React/Vite 应用；浏览器通过同源 `/api` 和 `/opentalking` 访问后端及 OpenTalking。开发和交付使用 Vite、Nginx、Docker Compose 与 CI 质量门禁。

## Capabilities and Constraints

- 支持文字、语音、视频模拟面试，服务端保存进度和答案状态，支持 AI 追问、评分、报告、按题录制与管理端鉴权回放。
- 支持简历上传、解析、岗位申请和 AI 岗位匹配；匹配依据应以业务可读的技能、优势、差距、风险和证据呈现，不直接把原始 JSON 作为用户界面。
- 支持 Java 17 算法练习与隔离判题、反馈工单、持久化站内通知、私有学习资料 PDF、阅读权限和个人批注。
- 必须保留候选人、企业管理员和平台管理员的角色权限边界；后端鉴权才是数据安全边界，前端可见性不能替代授权。
- 私有媒体、简历、录制、PDF 和批注不得进入前端构建产物；密钥、Token、`.env`、用户媒体和构建产物不得写入或提交到文档与源码。
- OpenTalking 是当前保留的数字人集成；avatar-skill 与讯飞链路保持禁用。OpenTalking 不可用时，面试仍应保留可用的浏览器语音降级路径。
- 现有前端使用 React 19、TypeScript、Vite、Tailwind CSS、Radix UI、Framer Motion、Lucide 和 Monaco Editor；改动应延续当前组件、路由级懒加载和同源代理边界。
- 数据库结构通过 Flyway 管理，当前有效迁移源为 `backend/src/main/resources/db/migration/`；UI 变更完成后应登记 `docs/CHANGELOG.md`，不改写已发布迁移。

## Brand Commitments

- 产品名为 AInterview。
- 当前品牌主标题为“每一面，都算数。”。
- 面向用户的界面文案以正式、简洁、可理解的中文为主；状态、错误、加载、空状态和操作反馈应清晰说明下一步。

## Evidence on Hand

- 产品入口与功能清单：`README.md`。
- 当前前端路由与角色壳：`frontend-react/src/app.tsx`、`frontend-react/src/components/`。
- 当前前端全局样式与设计令牌：`frontend-react/src/styles.css`。
- 项目边界和前端目录职责：`docs/project-structure.md`。
- 当前产品能力、验证记录和未完成事项：`docs/CHANGELOG.md`、`docs/iteration-plan.md`。
- 后端权限、媒体安全、招聘、学习资料和 AI 任务实现位于 `backend/src/main/java/com/tyut/aiinterview/` 及对应迁移目录。

## Product Principles

- 让练习结果可理解、可复盘、可行动，而不是只给出分数。
- 用真实业务状态和证据支撑 AI 结论，避免把不可读的原始数据直接交给用户。
- 默认保护候选人、企业和平台数据的权限边界与隐私。
- 在桌面和窄屏设备上保持核心任务可完成，优先保证内容层级、操作可达性和状态反馈。
- 让关键流程具备可靠保存、可恢复、可重试和明确降级路径。

## Accessibility & Inclusion

- 现有产品要求覆盖窄屏移动端，已将 390px 及更窄视口的无横向溢出、触控尺寸、长文本换行、抽屉安全区和可访问标签作为验收关注点。
- 交互组件优先沿用 Radix UI 等具备语义基础的组件，并为图标按钮、加载、错误、空状态和异步状态提供可理解文本。
- 正式的 WCAG 目标等级尚未由产品方确认；后续可将 WCAG 2.2 AA 作为统一验收标准再补入本节。
